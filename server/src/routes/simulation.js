import { Router } from 'express';
const router = Router();

// Stubs — implemented in Phase 6
router.get('/group/messages', (req, res) => res.status(501).json({ error: 'Not yet implemented' }));

export default router;
