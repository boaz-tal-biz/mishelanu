import { Router } from 'express';
const router = Router();

// Stubs — implemented in Phase 5
router.post('/parse', (req, res) => res.status(501).json({ error: 'Not yet implemented' }));

export default router;
